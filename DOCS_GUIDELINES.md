# TokenToken 文档开发规范

本规范适用于本仓库中的所有教程页面、首页入口、导航配置和图片素材。新增或修改文档前必须先阅读本文件；如果现有页面写法与本规范冲突，新内容以本规范为准，并在涉及的页面内完成必要修正。

## 一、开发前检查

1. 阅读本规范和需要修改的现有页面，不凭空设计新的格式。
2. 检查 `git status --short`，保留用户已有及与当前任务无关的修改。
3. 新教程应参考相邻页面的结构，同时保持文案简洁、面向非技术用户。
4. 页面源文件放在 `docs/guide/`；新页面通常还需要更新：
   - `docs/index.md`：首页入口。
   - `docs/.vitepress/config.mts`：侧边栏入口。

## 二、链接规范

### 1. 正文文字链接必须新开标签页

教程正文、首页表格、下载地址、官网和其他可点击文字链接，统一使用：

```html
<a href="/guide/codex" target="_blank" rel="noopener noreferrer">Codex 使用</a>
```

要求：

- 必须包含 `target="_blank"`，避免覆盖当前文档页面。
- 必须同时包含 `rel="noopener noreferrer"`。
- 链接文字必须简短、清晰，说明点击后会看到什么。
- 不得直接显示冗长原始 URL。
- 网盘链接使用“平台 + 用途 + 提取码”等可读标题，例如“百度网盘下载（提取码：1234）”。
- 站点目录锚点、VitePress 顶部导航和侧边栏配置按框架原生方式处理，不强制添加 `target="_blank"`。

正确示例：

```html
<a href="https://1token-store.com/keys" target="_blank" rel="noopener noreferrer">TokenToken API 密钥</a>
```

错误示例：

```md
https://1token-store.com/keys
```

### 2. 图片链接不得新开标签页

图片需要由全站图片浮窗接管，图片外层链接不得添加 `target="_blank"`：

```html
<a href="/images/example/example-01.webp"><img class="guide-image-compact" src="/images/example/example-01.webp" alt="选择配置方式"></a>
```

点击后应满足：

- 当前页面 URL 不变。
- 页面内弹出大图浮窗。
- 点击遮罩、关闭按钮或按 `Esc` 可以关闭浮窗。
- 不得跳转到图片地址或打开新标签页。

## 三、图片规范

### 1. 格式和目录

- 新增截图必须先压缩为 WebP 后再引用，正文不得新增 PNG/JPG 原图引用。
- 图片放在 `docs/public/images/<页面短名>/` 下，不放在教程 Markdown 同级目录。
- 新文件名使用简短 ASCII 小写名称和连续编号，例如 `codex-skin-01.webp`，避免空格和无意义名称。
- 可先把 PNG 放入 `docs/public/images/` 后运行 `npm run images:compress`；如果脚本未转换，必须使用 `sharp` 等工具手动生成 WebP 并检查文件大小。
- 转换完成后确认页面只引用 WebP；用户提供的原始素材不要擅自删除。

### 2. 页面展示尺寸

- 普通正文图片最大宽度由全站样式限制为 `560px`。
- 教程步骤截图默认使用 `class="guide-image-compact"`，最大宽度为 `480px`。
- 移动端图片最大宽度为容器宽度，不得产生横向滚动。
- 图片保持原始宽高比，使用 `object-fit: contain`，不得拉伸、裁切关键操作区域或占满整屏。
- 表格中的图片应控制高度，避免单张图片撑高整个页面。

### 3. 图片语义

- 每张图片必须有准确的 `alt`，描述截图中的操作或结果。
- 禁止使用 `alt="Image"`、`alt="图片"` 等无意义文本。
- 图片应紧跟对应步骤，不把所有截图堆在章节末尾。
- 一张图片只用于说明一个明确步骤；需要展示多个连续操作时，按顺序编号。

## 四、页面内容规范

- 每页只使用一个一级标题 `#`，标题直接说明主题。
- 主要流程使用二级标题 `##`，步骤使用有序列表。
- 指令、命令和用户需要输入的内容使用带语言标识的代码块，例如：

  ```text
  帮我安装这个工具
  ```

- FAQ 使用 `### 问题` 作为三级标题，便于目录展示和阅读。
- 导航和首页入口使用短标题；“教程”二字没有必要时应省略。
- 文案优先说明“当前要做什么”和“完成后会看到什么”，避免堆砌技术实现细节。
- 不在页面中展示 API 密钥、令牌或其他敏感信息。

## 五、样式和交互规范

- 复用 `docs/.vitepress/theme/custom.css` 和现有主题组件，不在单个页面重复实现浮窗。
- 全站图片浮窗由 `docs/.vitepress/theme/ImageLightbox.vue` 提供；新增图片必须使用可被该组件识别的图片链接结构。
- 不使用行内脚本，不为单个教程引入新的前端依赖。
- 新增样式应尽量使用可复用类，并同时检查桌面端和移动端表现。
- 页面元素不得重叠，长文字不得溢出容器，交互操作不得导致布局跳动。

## 六、完成后的验证

每次文档修改至少完成以下检查：

1. 运行 `git diff --check`，确认没有空白符或补丁格式问题。
2. 运行 `npm run docs:build`，确保 VitePress 构建成功。
3. 每次完成修改后，必须先停止正在运行的本地预览，再使用以下命令重新启动；不得只依赖热更新或刷新，避免网页仍展示旧内容：

   ```bash
   npm run docs:dev -- --force
   ```

4. 在浏览器中验证：
   - 新页面正文、首页入口和侧边栏入口正常。
   - 正文文字链接在新标签页打开。
   - 图片尺寸符合规范，图片已加载且没有变形。
   - 点击图片时 URL 不变，并出现图片浮窗。
   - 页面控制台没有新增警告或错误。
5. 如果重启本地预览后浏览器仍显示旧内容，再刷新缓存，不要直接判断代码未生效。

## 七、提交前清单

- [ ] 已阅读本规范。
- [ ] 已保留无关的用户修改。
- [ ] 新增文字链接使用可读标题，并在新标签页打开。
- [ ] 页面中没有显示冗长原始 URL。
- [ ] 新增图片已转换为 WebP。
- [ ] 教程截图使用合适的展示尺寸和有效 `alt`。
- [ ] 点击图片使用浮窗，未跳转新页面。
- [ ] 新页面已加入首页和侧边栏。
- [ ] `git diff --check` 通过。
- [ ] `npm run docs:build` 通过。
- [ ] 已停止旧的本地预览，并使用 `npm run docs:dev -- --force` 重新启动。
- [ ] 已按改动风险完成浏览器验证。
